interface:
  display_name: "Orchestrate Agents"
  short_description: "Route work across Claude, Codex, Grok, and Cursor"
  default_prompt: "Use /orchestrate-agents to delegate this task across cost-effective Claude, Codex, Grok, and Cursor workers."
policy:
  allow_implicit_invocation: false
metadata:
  version: "1.2.0"
