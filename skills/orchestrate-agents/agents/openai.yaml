interface:
  display_name: "Orchestrate Agents"
  short_description: "Route work across Claude, Codex, and Grok"
  default_prompt: "Use $orchestrate-agents to delegate this task across cost-effective Claude, Codex, and Grok workers."
policy:
  allow_implicit_invocation: false
metadata:
  version: "1.0.0"
